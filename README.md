# timeSheetApp

a [Sails](http://sailsjs.org) application

Install sails & gulp globally:-
- npm install sails -g
- npm install gulp -g

1) Run `npm install`.
2) Run `gulp`.
3) Run `sails lift` to run the app.
